USE CLOUDWICK;

set hive.enforce.bucketing=true;

CREATE TABLE IF NOT EXISTS BUCKET_DEPT(
	EMPID INT,
	EMPNAME STRING,
	DEPTNAME STRING
)
CLUSTERED BY (DEPTNAME) INTO 3 BUCKETS
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ',';

FROM EMPDEPT
INSERT INTO TABLE BUCKET_DEPT
SELECT EMPID,EMPNAME,DEPTNAME;
